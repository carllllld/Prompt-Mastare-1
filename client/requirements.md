## Packages
framer-motion | For smooth animations and transitions between sections
lucide-react | For beautiful icons (Sparkles, Copy, etc.)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
The application communicates with a single endpoint POST /api/optimize.
The backend handles the AI interaction.
The frontend needs to handle the loading state gracefully and present the result in a structured way.
